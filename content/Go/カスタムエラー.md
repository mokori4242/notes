---
title: カスタムエラー
date: 2025-09-26
modified: 2025-09-26
tags:
  - go
  - errorhandling
---
- [[センチネル(sentinel = 番兵・番人)エラーとは]]は異なり、構造体で定義されるエラー
  - 内部に任意の状態を含めることができる
```go
// io/fs package
type PathError struct {
	Op   string
	Path string
	Err  error
}

// io/fs package使用側
package main

import (
	"errors"
	"fmt"
	"io/fs"
	"os"
)

func main() {
	if _, err := os.Open("non-existing"); err != nil {
		var pathError *fs.PathError
		// As関数でerrに第二引数のカスタムエラーが含まれていればerrの値がインスタンスに代入される
		if errors.As(err, &pathError) {
			fmt.Println("Failed at path:", pathError.Path)
		} else {
			fmt.Println(err)
		}
	}

}
```

<hr>

## 参考
- https://pkg.go.dev/errors#example-As
- https://pkg.go.dev/io/fs#PathError
