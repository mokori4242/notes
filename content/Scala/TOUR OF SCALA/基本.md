[Scastie]( https://scastie.scala-lang.org/)を利用することでブラウザ上でScalaを実行することができる
<hr>

## 式
- 式とは評価すると結果が得られるもの
- `println`を使用することで、式の結果を出力できる
  ```scala
   println(1) // 1
   println(1 + 1) // 2
   println("Hello!") // Hello!
   println("Hello," + " world!") // Hello, world!
  ```   
## 値
 - `val`キーワードを利用することで、式の結果に名前をつけることができる
 ```scala
 val x = 1 + 1
 println(x) // 2
 ```
 - 上記のような`x`のように、名前をつけられた式の結果を値と呼び、式の結果に名前をつけているため、値を参照しても再計算はされない。また、値は再代入することもできない
 ```scala
 x = 3 // Reassignment to val x
 ```
 - 値の型は推論可能だが、明示的に型宣言することもできる
 ```scala
 val x: Int = 1 + 1
 ```
## 変数
- `var`キーワードを使うことで変数を定義できる。変数は再代入可能
- 値と同様に明示的に型宣言することもできる
```scala
var x = 1 + 1
println(x) // 2
x = 3
println(x) // 3
var y: Int = 2 + 2
println(y) // 4
```